{
  "$comment": "Dependencies the Phobos memory engine requires. The wizard reads this and runs `npm install` for each entry. Pinned to caret ranges for stability.",
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "@qdrant/js-client-rest": "^1.14.0"
  }
}
