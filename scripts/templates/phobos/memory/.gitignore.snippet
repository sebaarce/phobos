# Phobos memory engine — local-only state
vault/memory/.engine/.index-state.json

# Phobos memory engine — install aislado (deps locales, no se commitean).
# Crítico en proyectos no-Node (Rails/Python/Go) donde node_modules/ no
# suele estar excluido globalmente.
vault/memory/.engine/node_modules/
