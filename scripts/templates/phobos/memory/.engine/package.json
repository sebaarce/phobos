{
  "name": "phobos-memory-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phobos Memory engine — isolated install. Lives under vault/memory/.engine/ so the host project doesn't need its own package.json or node_modules. Same pattern as .codegraph/.",
  "dependencies": {
    "@xenova/transformers": "2.17.2",
    "@qdrant/js-client-rest": "1.14.0",
    "onnxruntime-node": "1.14.0"
  }
}
