{
  "$schema": "https://opencode.ai/config.json",
  "$comment": "Phobos — config de OpenCode. context7 (documentación de librerías, anónimo/read-only) queda disponible para los agentes que lo necesiten. En OpenCode los MCP remotos declarados acá están accesibles por default; el researcher y el programmer son los que lo aprovechan para chequear APIs de librerías actualizadas.",
  "mcp": {
    "context7": {
      "type": "remote",
      "url": "https://mcp.context7.com/mcp",
      "enabled": true
    }
  }
}